import COLOR from "../../constants/colors";

const styles = {
  paleBackground: {
    background: COLOR.white,
  },

  middle: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  },
};

export default styles;
